services:
  app:
    build: .
    ports:
      - "${PORT:-8080}:${PORT}"
    environment:
      - PORT=${PORT:-8080}
      - DATABASE_URL=${DATABASE_URL:-/app/data/sqlite.db}
      - MIGRATE=true
    volumes:
      - ./data:/app/data
